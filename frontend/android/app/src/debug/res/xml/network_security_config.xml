<?xml version="1.0" encoding="utf-8"?>
<!--
  Konfigurasi ini HANYA dipakai varian debug. Varian release memakai
  src/main/res/xml/network_security_config.xml yang menolak seluruh cleartext.

  `localhost`/`127.0.0.1` untuk jembatan `adb reverse` lewat USB, sedangkan
  alamat 192.168.x dipakai saat HP menghubungi laptop langsung lewat Wi-Fi.

  Android Network Security Config tidak mendukung notasi CIDR, jadi alamat
  laptop harus ditulis satu per satu. Kalau IP laptop berubah (mis. ganti
  jaringan), tambahkan alamat barunya di sini — kalau tidak, permintaan akan
  ditolak oleh sistem sebelum sempat keluar dari perangkat.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">192.168.8.99</domain>
    </domain-config>
</network-security-config>
